language: node_js
node_js:
  - "node"
install:
  - npm install
script:
  - npm test
cache:
  directories:
    - node_modules
install:
  - npm i && npm i -g firebase-tools
after_success:
  - test $TRAVIS_BRANCH = "master" && test $TRAVIS_PULL_REQUEST = "false" && firebase deploy --project airlivery-app --token $FIREBASE_TOKEN