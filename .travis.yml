language: node_js
node_js:
  - "node"
install:
  - npm install
script:
  - npm test
cache:
  directories:
    - node_modules
after_success:
  - npm run build
  - test $TRAVIS_BRANCH = "master" && test $TRAVIS_PULL_REQUEST = "false" && npx firebase deploy --project airlivery-app --token $FIREBASE_TOKEN